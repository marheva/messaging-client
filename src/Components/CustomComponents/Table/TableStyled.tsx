import styled from 'styled-components'
import TableBase from './TableBase'

const TableStyled = styled(TableBase).attrs({})`
`

TableStyled.displayName = 'CustomComponentStyledTable';
export default TableStyled
